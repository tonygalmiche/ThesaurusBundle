{% extends "OVEAuthentificationBundle:Default:ove-base.html.twig" %}
{% block content -%}
    <h1>Modification Terme</h1>
    <form action="{{ path('terme_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        </p>
        <button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-save"></span> Sauvegarder</button>
    </form>
    </p></p>
    <a type="button" class="btn btn-sm btn-default" href="{{ path('terme') }}"    ><span class="glyphicon glyphicon-list"></span> Retour à la liste</a></p>
    <a type="button" class="btn btn-sm btn-default" href="{{ path('terme_new') }}"><span class="glyphicon glyphicon-plus"></span> Ajouter un nouveau Terme</a></p></p>
    {{ form(delete_form) }}
{% endblock %}
